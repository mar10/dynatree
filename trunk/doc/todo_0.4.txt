== TODOs for 0.4 ==
(This file will be removed on release)

  - ltWait.gif and ltError.gif -> use css
  - implement onQueryActivate and onQueryExpand
  - implement  minExpandLevel > 1 
  - lazy re-read with [shift]:
		if( event.shiftKey && dtnode.isLazy )
			alert("ctrl");
  - BUG: Expander (and checkbox) click doesn't work on FF 2
  - BUG: context menu sample doesn't work in Opera
  - BUG: Chrome doesn't store cookies: persistence doesn't work
  - After clicking an expander icon the focus is gone
  - OPT: use opts.debugLevel instead of _bDebug
  - OPT: $(this.span) --> $span
  - option node.unselectable
  - options.hideCheckbox, class="hideCheckbox"    
  - issue #56: <span class="ui-dynatree-title"> --> use a template?
	tabTemplate: '<li><a href="#{href}"><span>#{label}</span></a></li>',
	var $li = $(o.tabTemplate.replace(/#\{href\}/g, url).replace(/#\{label\}/g, label));
	panelTemplate: "<div></div>"
	
  
  Test and doc
    - Test files for release: use minified libs 
    - rework samples with better names and title, header explanation
    - sample-options: mit reInit() und interaktiven options
    - Sample quick (und andere): schon beim LAden kommt ein onActivate event.
    - Homepage: IE shows Javascript errors (prettify.js?) and code is not pretty printed
    - Url-Tree as an own sample (currently in sample-events)
	- Mention $("#ui-dynatree-id-12").attr("dtnode").activate();
    
  - data.isFolder -> folder, .isLazy -> lazy 

=============================================================================

Later:
  - Prepare for jQuery 1.3
  - Optimize rendering (called too often)
  - Optimize html strucuture (don't nee two containers?) 
  - ISSUE: persistence doesn't work with lazy nodes
 	       E.g. open lazy node -> [F5] -> Node is marked open, but not expanded
  
  - Always hide root.
    Options .rootVisible and .title could be removed.
    If a single visible root is wanted, one could always create one using <ul>
    Top level node expanders are not connected by vlines (requires additional expander icon).
    If minExpandLevel>=1, also the expander icons are hidden, so the first icon colmun can be removed.
    
  - Prepare for drag'n'drop support:
    The jquery plugin must be easily integrated.
    Add functions node.insertBefore, node.insertAfter

  - Table-Tree: use CSS 2.1: display:table, display:table-row, display:table-cell
   
  - Tree <div> should behave like a control:
    when we Tab into the tree, the last focused node should be focused
    
  - Cookie configurable (allow more than session lifetime)
		cookie: {
			expires: 7, // Days or Date; null: session cookie
			path: "/", // Defaults to current page
			domain: "jquery.com",
			secure: true
		},
		
  - Implement Unit Testing: 
    http://www.jsunit.net/
    http://www.jsunit.net/talks/jsunit.ppt
    
  - Get rid of Protoype.js Class pattern. Use standard instead:
    http://www.crockford.com/javascript/private.html

  
=============================================================================

Changelog 0.3 -> 0.4
  - New ui.core.js 1.5.2 -> 1.6 rc2
  - issue #59: 'expand' -> 'expanded'
    data.select and expand are commands(verbs) class="expanded selected" are adjectives  
  - issue #54: class='selected expanded lazy folder'
    Note: Cookie persistence overrides 'expanded' 
    Note: data='...' overrides id, class, and title attributes
    Note: id="" must begin with a character.  
  - issue #53: 'selected'
  - issue # : tree.visit
  - tree.bind / unbind
  - tree.enable / disable
  - opts.onClick, ... to hook events
  - issue #63: work with prototype
  - distinguish 'active' and 'selected' 
  - issue #15: multi-select
  - issue #17: checkbox mode
  - use span + css backround-image: url()
  - issue # : use effects for expanding 
  - image paths configurable using css
  - issue #61: allow image names + extensions
  - onKeypress event wird in FF für [space] doppelt aufgerufen, weil erst keydown kommt
    http://www.quirksmode.org/js/keys.html
    --> alles auf keydown umstellen (?), denn das kommt immer
    Ausserdem charcode vermeiden und stattdessen keycode verwenden.
  - unselect removed
  - focusRoot
    use 'focus' style for initial focus
    use autoFocus for runtime behaviour
  - onDblClick(): lieber nicht den folder togglen; das sollte besser ein custom callback machen 
    (toggle() sollte dann aber sicherstellen, dass mandatory rootnodes nicht kollabieren).
  - selectionVisible --> activeVisible
  - expandOnAdd --> removed
  - rootCollapsible --> minExpandLevel
  - Grafik für Lazy-Expander verbessern
  
  - tree.isInitializing()
    easy way (or example) to ignore onActivate events during 'init' phase
  - tree.toDict(), node.toDict(recursive)
  